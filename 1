def closeEvent(self, evnt): 
    if self._closable: 
        super(App, self).closeEvent(evnt) 
    else: 
        evnt.ignore()
@pyqtSlot() 
def on_click(self): 
    self._closable = True 
    self.close()